diff --git a/node_modules/astro-spaceship/components/Article/utils/get-static-paths.ts b/node_modules/astro-spaceship/components/Article/utils/get-static-paths.ts
index 1234567..abcdefg 100644
--- a/node_modules/astro-spaceship/components/Article/utils/get-static-paths.ts
+++ b/node_modules/astro-spaceship/components/Article/utils/get-static-paths.ts
@@ -1,7 +1,7 @@
 import type { GetStaticPaths } from "astro";
 import { getCollection } from "astro:content";
 import { toUrl } from "astro-loader-obsidian";
-import { join } from "node:path";
+import { join } from "node:path/posix";
 
 import {
 	AUTHORS_COLLECTION_NAME,
@@ -129,9 +129,9 @@ export const getStaticPathsFactory = ({
   documentsCollection = DOCUMENTS_COLLECTION_NAME,
   tagsCollection = TAGS_COLLECTION_NAME,
 }: GetStaticPathsFactoryConfig) => (async () => {
-	const authors = (await getCollection(authorsCollection)) as Author[];
-	const documents = (await getCollection(documentsCollection)) as Document[];
-	const tags = (await getCollection(tagsCollection)) as Tag[];
+	const authors = (await getCollection(authorsCollection as any)) as Author[];
+	const documents = (await getCollection(documentsCollection as any)) as Document[];
+	const tags = (await getCollection(tagsCollection as any)) as Tag[];
 
 	const tree = buildTree(documents as Document[]);
 

